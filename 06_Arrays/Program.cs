class arrays
{
    static void main(string[] args)
    {
        
    }
// Arrays are fixed size elemts of same datatype strored in contigues way in memeory locaiton

    void Arrays()
    {
        int[] arr = new int[4];
        for(int i = 0 ; i < arr.Length ; i++)
        {
            
        }
    }
}